<IfModule mod_rewrite.c>
    RewriteEngine On
    # Right htaccess rules
    RewriteRule ^(.*)$ public/$1 [L]
    # Redirect Trailing Slashes...
    # RewriteRule ^(.*)/$ public/$1 [L,R=301]

</IfModule>
